<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Homepage</title>
    <style>
      body {
        font-family: arial;
        margin: 0;
      }
      nav {
        background: rgb(71, 154, 221);
        padding: 20px 10px;
      }
      nav ul {
        max-width: 960px;
        margin: 0 auto;
        padding: 0;
      }
      nav li {
        list-style-type: none;
        display: inline-block;
        margin: 0 10px 0 0;
      }
      nav a {
        color: #fff;
        font-size: 18px;
        background: rgba(255, 255, 255, 0.2);
        text-decoration: none;
        padding: 10px;
        display: block;
      }
      main,
      header {
        max-width: 960px;
        margin: 30px auto;
        padding: 0 10px;
        color: #333;
      }
      main img {
        width: 40px;
        margin-right: 10px;
        position: relative;
        top: 12px;
      }
      main p {
        display: inline-block;
      }
    </style>
  </head>
  <body>
    <nav>
      <ul>
        <% if (user) { %>
        <li><a href="/auth/logout">Log out</a></li>
        <% } else { %>
        <li><a href="/auth/login">Login</a></li>
        <% } %>
        <li><a href="/">Homepage</a></li>
        <% function click() { if (!user){ alert("Please login first!") }} %>
        <li><a onclick="click()" href="/profile">Profile</a></li>
        <li>
          <a href="https://facebook.com" target="_blank">Go to Facebook</a>
        </li>
        <li>
          <a href="https://facebook.com/profile" target="_blank">Your wall</a>
        </li>
        <li><a href="/image">get random images for fun</a></li>
      </ul>
    </nav>

    <header>
      <img src="https://picsum.photos/1100/600/?random" alt="image.png" />
    </header>
    <main></main>
  </body>
</html>
